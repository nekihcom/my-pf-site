import { FC, memo } from "react";

export const Header:FC = memo(() => {

    return (
        <>
        <header className="text-center font-bold">
            <h1>Mochiken.Tech</h1>
        </header>
        </>
    )
});